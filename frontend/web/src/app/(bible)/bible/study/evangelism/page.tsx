'use client';

import React from 'react';
import { EvangelismWorkspace } from '../../../features/evangelism';

export default function EvangelismStudyPage() {
  return (
    <div className="w-full max-w-5xl mx-auto space-y-4 animate-in fade-in duration-200">
      <EvangelismWorkspace />
    </div>
  );
}
